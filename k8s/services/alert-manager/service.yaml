apiVersion: v1
kind: Service
metadata:
  name: alert-manager-service
  namespace: costwatch
  labels:
    app: alert-manager
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 5001
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: alert-manager