apiVersion: v1
kind: Service
metadata:
  name: analytics-engine-service
  namespace: costwatch
  labels:
    app: analytics-engine
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 5002
    targetPort: 5002
    protocol: TCP
    name: http
  selector:
    app: analytics-engine