apiVersion: v1
kind: Service
metadata:
  name: analytics-engine-service
  namespace: costwatch
  labels:
    app: analytics-engine
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 8003
    targetPort: 8003
    protocol: TCP
    name: http
  selector:
    app: analytics-engine