apiVersion: v1
kind: Service
metadata:
  name: resource-scanner-service
  namespace: costwatch
  labels:
    app: resource-scanner
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: resource-scanner